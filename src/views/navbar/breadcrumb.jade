ul.mu-breadcrumb-list
  li.mu-breadcrumb-list-item.brand
    a(href='#')
      i.mu-font.logo
  li.mu-breadcrumb-list-item(ng-repeat='crumb in breadcrumb.list()', ng-class='{ "last": $last }')
    a(href='', ng-click='breadcrumb.go(crumb.state)') {{ crumb.name | translate }}
